# Python (validate-release.py / golden_rules.py)
__pycache__/
*.py[cod]
*$py.class

# macOS
.DS_Store

# Editor
.idea/
.vscode/
*.swp

# Claude Code per-machine local settings (allowlist is user-specific, not portable)
.claude/settings.local.json

# CCteam runtime artifacts — created by this skill in user projects, must never appear in source repo
.plans/
